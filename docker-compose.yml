version: "3.8"

services:
  mysql:
    platform: linux/x86_64 # no ARM image available yet
    image: mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: example
